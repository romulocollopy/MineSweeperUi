schema {
  query: Query
}

type Query {
  viewer: User
  mineSweeper(slug: String!): MineSweeper!
}

type User {
  id: ID!
  name: String!
  email: String!
}

type MineSweeper {
  slug: String!
  blocks: [Block!]!
}

type Block {
  coordinates: Coordinates!
  display: String!
}

type Coordinates {
  x: Int!
  y: Int!
}
